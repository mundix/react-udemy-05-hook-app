import {createContext} from 'react';

// Higher order component, como el swith como el router compoentns 
// Eso queire decir que puedo colocar componentes hijos
export const UserContext  = createContext(null);
